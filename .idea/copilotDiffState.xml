<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/Task (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/Task (snapshot).java" />
              <option name="originalContent" value="package com.likhith.taskengine.model;&#10;&#10;import jakarta.persistence.*; // Java Persistence API for DB mapping&#10;import jakarta.validation.constraints.NotBlank; // For data validation&#10;import lombok.AllArgsConstructor; // Lombok: Generates constructor with all fields&#10;import lombok.Data; // Lombok: Generates Getters, Setters, toString, and Equals&#10;import lombok.NoArgsConstructor; // Lombok: Generates empty constructor (required by Hibernate)&#10;&#10;@Entity // Marks this class as a Database Table&#10;@Table(name = &quot;tasks&quot;) // Explicitly names the DB table &quot;tasks&quot;&#10;@Data&#10;@NoArgsConstructor&#10;@AllArgsConstructor&#10;public class Task {&#10;&#10;    @Id // Defines the Primary Key for the table&#10;    @GeneratedValue(strategy = GenerationType.IDENTITY) // Database will auto-increment this ID (1, 2, 3...)&#10;    private Long id;&#10;&#10;    @NotBlank(message = &quot;Title cannot be empty&quot;) // Validation: API will return 400 Error if title is null/empty&#10;    private String title;&#10;&#10;    private String description;&#10;&#10;    @Enumerated(EnumType.STRING) // Tells DB to store &quot;TODO&quot; (String) instead of 0 (Integer)&#10;    private TaskStatus status = TaskStatus.TODO; // Default value is TODO&#10;}" />
              <option name="updatedContent" value="package com.likhith.taskengine.model;&#10;&#10;import jakarta.persistence.*; // Java Persistence API for DB mapping&#10;import jakarta.validation.constraints.NotBlank; // For data validation&#10;import jakarta.validation.constraints.Size; // For size validation&#10;import lombok.AllArgsConstructor; // Lombok: Generates constructor with all fields&#10;import lombok.Data; // Lombok: Generates Getters, Setters, toString, and Equals&#10;import lombok.NoArgsConstructor; // Lombok: Generates empty constructor (required by Hibernate)&#10;&#10;@Entity // Marks this class as a Database Table&#10;@Table(name = &quot;tasks&quot;) // Explicitly names the DB table &quot;tasks&quot;&#10;@Data&#10;@NoArgsConstructor&#10;@AllArgsConstructor&#10;public class Task {&#10;&#10;    @Id // Defines the Primary Key for the table&#10;    @GeneratedValue(strategy = GenerationType.IDENTITY) // Database will auto-increment this ID (1, 2, 3...)&#10;    private Long id;&#10;&#10;    @NotBlank(message = &quot;Title cannot be empty&quot;) // Validation: API will return 400 Error if title is null/empty&#10;    @Size(min = 1, max = 100, message = &quot;Title must be between 1 and 100 characters&quot;)&#10;    private String title;&#10;&#10;    @Size(max = 500, message = &quot;Description must not exceed 500 characters&quot;)&#10;    private String description;&#10;&#10;    @Enumerated(EnumType.STRING) // Tells DB to store &quot;TODO&quot; (String) instead of 0 (Integer)&#10;    private TaskStatus status = TaskStatus.TODO; // Default value is TODO&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/TaskService (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/TaskService (snapshot).java" />
              <option name="originalContent" value="package com.likhith.taskengine.service;&#10;&#10;import com.likhith.taskengine.model.Task;&#10;import com.likhith.taskengine.repository.TaskRepository;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.stereotype.Service;&#10;&#10;import java.util.List;&#10;&#10;import static org.springframework.data.jpa.domain.AbstractPersistable_.id;&#10;&#10;@Service // 1. Marks this as the &quot;Brain&quot; of the application&#10;@RequiredArgsConstructor // 2. Lombok: creates a constructor for the 'final' fields below&#10;public class TaskService {&#10;&#10;    private final TaskRepository taskRepository; // 3. The dependency we need&#10;&#10;    public Task createTask(Task task) {&#10;        // 4. Logic: We could check if a task with the same name exists here&#10;        return taskRepository.save(task); // 5. Pass it to the Data Layer&#10;    }&#10;&#10;    public List&lt;Task&gt; getAllTasks() {&#10;        return taskRepository.findAll(); // 6. Fetch everything from the DB&#10;&#10;        /*Key Concept: SoC (Separation of Concerns)&#10;           Why a Service? If tomorrow you decide to send a Slack notification every&#10;            time a task is created, you only change the Service.&#10;          The Controller and Repository stay exactly the same.*/&#10;    }&#10;        public Task saveTask(Task task){&#10;            return taskRepository.save(task);&#10;&#10;        }&#10;&#10;        public void deleteTask(Long id){&#10;            taskRepository.deleteById(id);&#10;        }&#10;&#10;}&#10;" />
              <option name="updatedContent" value="package com.likhith.taskengine.service;&#10;&#10;import com.likhith.taskengine.model.Task;&#10;import com.likhith.taskengine.repository.TaskRepository;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.stereotype.Service;&#10;&#10;import java.util.List;&#10;&#10;import static org.springframework.data.jpa.domain.AbstractPersistable_.id;&#10;&#10;@Service // 1. Marks this as the &quot;Brain&quot; of the application&#10;@RequiredArgsConstructor // 2. Lombok: creates a constructor for the 'final' fields below&#10;public class TaskService {&#10;&#10;    private final TaskRepository taskRepository; // 3. The dependency we need&#10;&#10;    public Task createTask(Task task) {&#10;        // 4. Logic: We could check if a task with the same name exists here&#10;        return taskRepository.save(task); // 5. Pass it to the Data Layer&#10;    }&#10;&#10;    public List&lt;Task&gt; getAllTasks() {&#10;        return taskRepository.findAll(); // 6. Fetch everything from the DB&#10;&#10;        /*Key Concept: SoC (Separation of Concerns)&#10;           Why a Service? If tomorrow you decide to send a Slack notification every&#10;            time a task is created, you only change the Service.&#10;          The Controller and Repository stay exactly the same.*/&#10;    }&#10;&#10;    public Task getTaskById(Long id) {&#10;        return taskRepository.findById(id)&#10;            .orElseThrow(() -&gt; new RuntimeException(&quot;Task not found with id: &quot; + id));&#10;    }&#10;&#10;    public Task saveTask(Task task){&#10;        return taskRepository.save(task);&#10;&#10;    }&#10;&#10;    public void deleteTask(Long id){&#10;        taskRepository.deleteById(id);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>